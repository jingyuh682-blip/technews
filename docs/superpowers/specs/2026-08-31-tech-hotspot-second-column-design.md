# 科技热点第二列调整设计

**日期：** 2026-08-31

## 背景

仪表盘现有四列为“词云/读书洞察、科技新闻/论文热点、科技热点、GitHub 高星/近一周上涨”。用户要求科技热点显示在第二列。

用户截图右下角的小机器人经页面 DOM、静态资源和项目源码核查，不是 TechNews 生成的元素；它属于浏览器或远程控制界面的鼠标指针外观。因此本次页面代码不新增、删除或拖拽该外部元素。

## 目标布局

```
第一列          第二列        第三列              第四列
热点词云        科技热点      科技新闻            GitHub 高星
图书洞察        科技热点      论文热点            近一周上涨
```

科技热点跨越两行；第一列和第四列保持上下分栏；新闻与论文热点整体顺移到第三列。

## 实现边界

- 只修改独立的 `public/layout-editorial-grid.css` 和其测试。
- 保持 0.86fr / 1.08fr / 1.08fr / 1.08fr 的列宽比例不变。
- 不修改 `public/styles.css`、`public/app.js`、`public/index.html` 等现有未提交业务改动。
- 不在页面代码中处理外部鼠标指针外观。
- 所有修改、测试、Git 提交和推送均在阿里云服务器执行。

## 验收条件

1. CSS 网格区域严格为 `"cloud hot news ghs" "books hot papers ghr"`。
2. 自动化回归测试覆盖上述列序与“科技热点跨两行”关系。
3. 项目现有测试均通过，且仅提交本次新建/修改的独立文件。